<!DOCTYPE html>
<html>
  <head>
    <title>My Address Book || HomePage</title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
  </head>
  <body>
    <head>
      <h1 class="text-blue-500 text-5xl my-3 w-4/12 mx-auto text-center p-5">
        My Address Book
      </h1>
    </head>
    <main
      class="text-center bg-blue-300 bg-cover bg-center min-h-auto mx-auto w-3/4 p-5 rounded-xl"
    >
      <div class="border-4 border-solid border-white w-3/5 mx-auto mt-7">
        <% for(const contact of results) {%>
        <article
          class="flex justify-between items-center bg-white w-80 mx-auto rounded-3xl m-5"
        >
          <a
            href="/contact/<%= contact.name %>"
            class="block text-xl text-blue-300 m-5 px-3 hover:scale-125 ease-in duration-200"
          >
            <p class="text-white text-2xl">
              <%= displayFormattedDate(contact.birthday) %>
            </p>
            <%= contact.name %> <%= contact.firstname %>
          </a>
          <div>
            <a
              href="/contact/update/<%= contact.name %> "
              class="inline-block px-3 hover:scale-150"
              >✏️</a
            >
            <form action="/contact/delete/<%= contact.name %> " method="post">
              <button type="submit" class="hover:scale-150">❌</button>
            </form>
          </div>
        </article>
        <% } %>
      </div>
      <a
        href="/contact/add"
        class="bg-blue-300 hover:bg-white text-white hover:text-blue-300 p-2 px-6 my-20 rounded border-solid border-2 border-white hover:border-blue-300 hover:shadow-inner text-xl"
      >
        Add Contact
      </a>
    </main>
    <script src="/public/javascripts/dateFormat.js"></script>
  </body>
</html>
